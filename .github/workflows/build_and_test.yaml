name: build_and_test
on: push
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - name: install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes ninja-build g++ python3 pip
          pip install conan
      - name: setup conan project
        run: conan install --output-folder ${{github.workspace}}/build --build-missing ${{github.workspace}}
      - name: configure cmake
        run: cmake -B ${{github.workspace}}/build -GNinja
      - name: build
        run: cmake --build ${{github.workspace}}/build
  # test:
  #   needs: build
  #   runs-on: ubuntu-22.04
  #   steps:
